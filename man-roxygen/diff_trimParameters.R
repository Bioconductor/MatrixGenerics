#' @param diff An integer specifying the order of difference.
#' @param trim A double in \[0,1/2\] specifying the fraction of observations to
#'   be trimmed from each end of (sorted) x before estimation.
